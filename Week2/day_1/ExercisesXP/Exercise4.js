let c;
    let a = 34;
    let b = 21;
    console.log(a+b) //first expression
    //Prediction:It will output 55 ,because 34 and 21 are the numbers
    //Actual:55
    a = 2;

    console.log(a+b) //second expression
    // Prediction:It will output 23,because the new value of a is 2
    // Actual:23

    //the value of c is undefined because c content no value

    console.log(3 + 4 + '5');
    //the result is 75,there an additional between 3 and 4 and the 5 is concat with the result of 3 and 4
    